# nodejs-agents

Node.js expert subagents for Claude Code.

## Agents

- **nodejs-expert** — Event loop, memory leaks, promises, module resolution, streams, HTTP servers
- **cli-expert** — CLI tool development, npm binaries, argument parsing, cross-platform compatibility

## Install

```
/plugin marketplace add GoogilyBoogily/googilyboogily-claude-power-tools --plugin nodejs-agents
```
