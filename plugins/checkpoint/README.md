# checkpoint

Lightweight code checkpoint management using git stash.

## Commands

- `/checkpoint:create` — Create a named checkpoint of current changes
- `/checkpoint:list` — List all saved checkpoints
- `/checkpoint:restore` — Restore a previously saved checkpoint

## Install

```
/plugin marketplace add GoogilyBoogily/googilyboogily-claude-power-tools --plugin checkpoint
```
